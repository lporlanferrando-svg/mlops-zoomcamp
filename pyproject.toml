[project]
name = "mlops-zoomcamp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.13"

dependencies = [
    "jupyter",
    "notebook",
    "pandas",
    "pyarrow",
    "scikit-learn>=1.9.1",
    "seaborn>=0.13.2",
]

[project.scripts]
mlops-zoomcamp = "mlops_zoomcamp:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
